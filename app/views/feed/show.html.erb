<h1>LeisureSphere</h1>
<p>The social media platform to share all the little things you are passionate about!</p>

<%= link_to "Write A Post", new_post_path %> 
<%= link_to "Post An Image", new_image_post_path %>

<div class="feed-items">
  <% if @feed_items.any? %>
    <% @feed_items.each do |item| %>
      <% if item.class == Post %>
        <div>
      	  <%= render "posts/post", post: item %>
        </div>
			<% elsif item.class == ImagePost %>
				<div>
          <%= render "image_posts/image_post", image_post: item %>
        </div>
			<% end %>
    <% end %>
  <% else %>
    <p>No posts from you or your connections were found.</p>
  <% end %>
</div>